# PR7418 configuration comparison

Run date: 2026-09-17 (UTC). Both the initial 19-case matrix and the additional
19 recipe-inspired cases are complete at the pinned revisions. Results for the
expansion are recorded separately below; 38 cases in total.

## Pinned inputs

| Role | Revision |
| --- | --- |
| Main when this comparison started | `cfa9e20d2658844dd84a07f2ccd34d7e62ab5552` |
| PR7418 head | `62a0a6c05640f28dfbbe3ee8c2bd11e7818b709a` |
| PR branch point, separate attribution control | `54c62dfb6852e9218bf38590be9b90930bf7d589` |
| Published harness | `fb49e22797fbc928edb39739aeb86b9b00cdafcf` |

Both sides used the same development environment: Python 3.12.3,
PyTorch `2.13.0a0+8145d630e8.nv26.06`, CUDA 13.3, H100 GPUs.
Single-rank cases used one GPU; the distributed case used eight GPUs with
TP=2, PP=2, DP=2. Inputs were mock training data, a local 261-token HF tokenizer,
and a tiny checkpoint generated by the baseline of each comparison.
No remote model or external CI artifact download was required.

## Results

| Comparison | Coverage | Outcome |
| --- | --- | --- |
| Main vs PR7418 | All 19 manifest cases, including eight-rank runtime | 18 complete captures with the six upstream differences below; one expected CLI rejection; zero execution/capture errors. |
| PR branch point vs PR7418 | All 18 single-GPU cases | 17 exact passes, including fresh training and resume; one expected CLI rejection; zero differences or execution/capture errors. |

A mismatch is not a pass: the comparator exits nonzero for it, even when its cause
is understood. No comparison field is waived. These results do not establish strict
equality against current main; that requires reconciling the upstream changes and
rerunning against the resulting PR head.

Fresh runtime cases trained through iteration 2, evaluated and saved a checkpoint.
Resume cases loaded the same baseline checkpoint at iteration 2, trained iterations
3–4 and evaluated. The final step-schedule check observed global batch size 2 at
7 consumed samples and 4 at 8 and 9 samples on both revisions.

The initial main/head runs used a draft snapshot of this harness. The published
version adds the `checkpoint_cli` case, adjacent step-transition observations,
checkpoint fixture hashes, and required checks for already-captured train/validation
configs. The changed cases were rerun against main/head; the single-GPU branch-point
control uses the published version throughout. Each baseline/candidate pair uses
identical capture/comparison code; per-file hashes are retained in its report.
The added train/validation completeness checks were also verified against all
successful archived captures, including every distributed rank. The eight-rank
case was run for main/head, not repeated for the branch-point control.

## Main/head differences and attribution

The completed comparisons expose six configuration fields changed on main since
the PR branched. These differences are retained in the report, not ignored:

| Field | Main/head difference | Upstream change |
| --- | --- | --- |
| `expert_data_parallel_sharding_strategy` | Main resolves the default to `optim_grads_params`; head retains `None`. | [PR7277](https://github.com/NVIDIA/Megatron-LM/pull/7277), commit `8ea4bb7a6` |
| `gdn_kernel_backend` | Absent on main, present on head. | [PR7326](https://github.com/NVIDIA/Megatron-LM/pull/7326), commit `1e137ddec` |
| `moe_router_aux_loss_fusion` | Present on main, absent on head. | [PR6967](https://github.com/NVIDIA/Megatron-LM/pull/6967), commit `0e48cdf3e` |
| `moe_shortcut_connection`, `moe_shortcut_parallel`, `moe_shortcut_post_norm` | Present on main, absent on head. | [PR6959](https://github.com/NVIDIA/Megatron-LM/pull/6959), commit `088cd1d76` |

These produce six paths per builder case, 17 paths per single-rank runtime case
(including repeated fields at actual model/DDP constructors), and 136 paths across
the eight-rank runtime case. The branch-point comparison is a separate control;
it never substitutes for the requested current-main comparison.

## Coverage and limits

The manifest exercises GPT and Hybrid configuration, known and tokenizer-derived
vocabulary, padding/no-padding, optimizer/DDP overrides, iteration/sample schedulers,
step and legacy batch schedules, data masking, local logging, checkpoint flag
translation/argument precedence, fresh training, actual checkpoint resume, TP/PP/DP,
and a deliberately conflicting CLI combination.

Builder cases use the real configuration and dataset builders, but a minimal
optimizer parameter-group fixture for scheduler derivation. Runtime cases use
actual training consumers. Checkpoint-argument cases check existing per-field
precedence, not a blanket assumption that checkpoint arguments override all CLI values.

Nine harness self-tests pass, including deliberately changed vocabulary/scheduler/DDP
values, missing captures/ranks, unsupported objects, and both-programs-failed handling.
Targeted isort/Black/ruff checks pass. Repository-wide pre-commit was also run in a
disposable clone; it reported pre-existing main formatting/style failures outside
these files. No unrelated formatter changes were retained.

This is configuration/input equivalence coverage, not a convergence or performance
gate. Short training/save/resume completion does not establish full numerical
equivalence. Logger/checkpoint captures are not an exhaustive audit of every consumer.
Neither PR7418 nor upstream main was modified. Raw logs and environment-specific
artifacts are not published; each local `report.json` contains source trees, per-file
harness hashes, fixture hashes, case outcomes, and every field difference.

## Recipe-matrix expansion

The [19 additional scenarios](SCENARIOS.md) use the same pinned main/head revisions
and development environment. All 19 additions completed on both revisions: twelve
builders, three single-rank runtime cases and four two-rank runtime cases. There are
zero execution/capture errors in the corrected runs and 19 strict mismatches confined
to attributed upstream fields. Across both delivery stages, all 38 cases ran: 37
strict mismatches and one expected CLI rejection. This is not a strict equality pass.

The explicit Mamba hybrid-stack case exposes two further upstream schema changes:

| Field under `hybrid_stack_spec.submodules` | Main/head difference | Upstream change |
| --- | --- | --- |
| `gdn2_layer` | Present on main, absent on head | [PR7311](https://github.com/NVIDIA/Megatron-LM/pull/7311), commit `a1a09b084` |
| `mla_fused_down_proj_layer` | Present on main, absent on head | [PR7304](https://github.com/NVIDIA/Megatron-LM/pull/7304), commit `42b8a6911` |

The Hybrid spec/block files are unchanged between the PR branch point and PR head.
This case has eight difference paths; the other completed added builders have six.
Full-recompute/fine-tune runtime cases have 17 paths each; MoE EP2, CP2, and TP2/SP
have 34 each; the interleaved PP2/VPP2 case has 56 because multiple model/DDP chunks
are captured on each rank. The VLM case has 27 paths, including its separate vision,
projection and language constructor configs. Fine-tuning restored weights at iteration 0 and trained
through iteration 2 on both sides, independently of the initial resume case.
The actual VLM runs completed training/evaluation with frozen vision encoders, 28×28
mock images, 32 text tokens and 37 decoder positions; the required VLM constructor,
freeze-policy and multimodal dataset captures are present on both sides.

Draft-to-final corrections, not waived failures:

- Distributed-optimizer layouts contain live Parameter keys. The first draft rejected
  them. The corrected adapter preserves all layout fields and describes references by
  model-local parameter name, shape, dtype and trainability. All four distributed
  cases were rerun with that adapter; tensor values are not compared.
- The source Qwen-Next recipe contains obsolete attention/weight-decay selectors;
  their current equivalents are documented in SCENARIOS.md.
- FSDP2 requires a different CUDA connection setting, untied embeddings, and disabled
  gradient-accumulation fusion. Both sides rejected the incomplete draft; the corrected
  configuration completes. This remains a builder-only test.
- The VLM draft mixed mutually exclusive common/encoder layer-depth flags. The corrected
  case uses explicit encoder and decoder depths and completed its rerun.

The original single-GPU expansion run and subsequent corrected-case reruns have
different harness/manifest hashes, retained in their private reports. Every compared
baseline/candidate pair uses identical harness code and fixtures. The original
branch-point control above was not repeated for the expanded matrix; source-level
attribution does not turn main/head mismatches into exact passes.

Thirteen final harness self-tests and targeted isort/Black/ruff checks pass. Full
repository pre-commit was rerun in a disposable container clone: existing baseline
formatting/pylint failures remain outside the harness, with no unrelated changes
retained. A workstation attempt additionally hit its old Git's unsupported
`ls-files --deduplicate`; the container run supplied the repository-wide check.
